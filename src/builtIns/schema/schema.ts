import { createSchema } from "../../definitions/schema";
import {
  dateToNumber,
  dateToString,
  ddmmyyyy,
  ddyyyymm,
  getDate,
  getDay,
  getFullYear,
  getHours,
  getMilliseconds,
  getMinutes,
  getMonth,
  getSeconds,
  getTime,
  getTimezoneOffset,
  getUTCDate,
  getUTCFullYear,
  getUTCHours,
  getUTCMilliseconds,
  getUTCMinutes,
  getUTCMonth,
  getUTCSeconds,
  mmddyyyy,
  toDateString,
  toISOString,
  toJSON,
  toLocaleDateString,
  toLocaleString,
  toLocaleTimeString,
  toTimeString,
  toUTCString,
  yyyyddmm,
} from "../operations/date";
import {
  acos,
  acosh,
  add,
  asin,
  asinh,
  atan,
  atan2,
  atanh,
  cbrt,
  ceil,
  clz32,
  cos,
  cosh,
  divide,
  exp,
  expm1,
  floor,
  fround,
  hypot,
  imul,
  log,
  log10,
  log1p,
  log2,
  max,
  min,
  multiply,
  numberToString,
  power,
  random,
  root,
  round,
  sign,
  sin,
  sinh,
  sqrt,
  square,
  subtract,
  tan,
  tanh,
  toExponential,
  toFixed,
  toPrecision,
  trunc,
} from "../operations/number";
import {
  base64Decode,
  base64Encode,
  capitalize,
  length,
  padEnd,
  padStart,
  repeat,
  replace,
  slice,
  strToNumber,
  toDate,
  toLower,
  toUpper,
  trim,
  truncate,
  urlDecode,
  urlEncode,
} from "../operations/string";
import { date, number, string } from "../types/types";
export const schema = createSchema(
  {
    types: [string, number, date],
    defaultType: "string",
  },
  {
    string: [
      base64Decode,
      base64Encode,
      capitalize,
      length,
      padEnd,
      padStart,
      repeat,
      replace,
      slice,
      strToNumber,
      toDate,
      toLower,
      toUpper,
      trim,
      truncate,
      urlDecode,
      urlEncode,
    ],
    number: [
      acos,
      acosh,
      add,
      asin,
      asinh,
      atan,
      atan2,
      atanh,
      cbrt,
      ceil,
      cos,
      clz32,
      cosh,
      divide,
      exp,
      expm1,
      floor,
      fround,
      hypot,
      imul,
      log,
      log10,
      log1p,
      log2,
      max,
      min,
      multiply,
      numberToString,
      power,
      random,
      root,
      round,
      sign,
      sin,
      sinh,
      sqrt,
      subtract,
      tan,
      tanh,
      square,
      toExponential,
      toFixed,
      toPrecision,
      trunc,
    ],
    date: [
      dateToNumber,
      dateToString,
      ddmmyyyy,
      ddyyyymm,
      getDate,
      getDay,
      getFullYear,
      getHours,
      getMilliseconds,
      getMinutes,
      getMonth,
      getSeconds,
      getTime,
      getTimezoneOffset,
      getUTCDate,
      getUTCFullYear,
      getUTCHours,
      getUTCMilliseconds,
      getUTCMinutes,
      getUTCMonth,
      getUTCSeconds,
      mmddyyyy,
      toDateString,
      toISOString,
      toJSON,
      toLocaleDateString,
      toLocaleString,
      toLocaleTimeString,
      toTimeString,
      toUTCString,
      yyyyddmm,
    ],
  }
);
